# Copy this to secrets.toml and replace the hash with your own.
# Generate a hash: python -c "from auth import hash_password; print(hash_password('your-password'))"

[passwords]
admin = "$2b$12$REPLACE_WITH_YOUR_BCRYPT_HASH"
